[build-system]
requires = ["Cython>=0.29.14",
            "scipy>=1.5",
            "oldest-supported-numpy",
            "setuptools<60",
            "wheel"]
build-backend = "setuptools.build_meta"
